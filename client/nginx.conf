server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Cần dòng này để React Router hoạt động đúng
        try_files $uri $uri/ /index.html;
    }
}